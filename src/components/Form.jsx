import React from 'react'
import styled from '@emotion/styled'

const InputSubmit = styled.input`
    
`

const Form = () => {
  return (
    <form>
        <InputSubmit 
            type="submit"
            value="Check"
        />
    </form>
  )
}

export default Form